
import { render } from "@testing-library/svelte";
import { fireEvent } from "@testing-library/svelte/node_modules/@testing-library/dom";
import PasswordRepeatInput from "../users/PasswordRepeatInput";


describe("ErrorsBox", () => {
    /*
        it('renders a header and assosicated value', async () => {
            const { container, debug, getByLabelText } = render(PasswordRepeatInput, { password: 'heading', error: [] });
            debug()
            console.log(getByLabelText('Password').innerHTML)
            fireEvent.change(getByLabelText('Password'), { target: { value: '' } })
            expect(container.getElementsByClassName('spectrum-Heading').length).toBe(1);
            expect(container.getElementsByClassName('spectrum-Heading')[0].textContent).toContain('heading value')
        })
    
        
            it('Error', async () => {
                const { container } = render(PasswordRepeatInput, { type: 'body', value: 'body value' });
                expect(container.getElementsByClassName('spectrum-Body').length).toBe(1);
                expect(container.getElementsByClassName('spectrum-Body')[0].textContent).toContain('body value')
            })
            */

})